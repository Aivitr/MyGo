对于这样一个循环：

```go
	testStr := "hello,世界"
	for idx := range testStr {fmt.Print(idx)}
    //共循环8次，依次输出0 1 2 3 4 5 6 9

```

有这样的结果实际上是因为 idx 代表的是**起始字节的索引**。UTF-8 对英文和标点采用的是一字节编码，但对中文采用的是三字节编码，当遍历到“世”字时，起始索引为 6，因此输出结果为 6 。但由于这个汉字占据了 6 7 8 三个字节，因此下一个循环里 idx 的值就直接从 9 开始了。

